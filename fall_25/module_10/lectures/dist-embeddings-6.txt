(gentle music) - We will now look at a neural net method, for obtaining embeddings of items, based only on qualitative similarity information. So the application here is checking signatures for forgery. Here's the overall model. At the bottom over here, a particular signature is fed in, and it's passed through several layers, of neural net processing. Let's call this neural net phi. The embedding of this signature, is taken to be the activations of the very last layer, of the neural net. So we have an embedding of that signature. On the right, another signature is fed in. It is passed through exactly the same neural net, and once again, the embedding is given, by the activations of the top layer. Now, the similarity, between the two signatures is simply taken, to be the co-sign similarity between these two embeddings, between these two vectors at the top. And so overall, the similarity function, computed by this model is the co-sign similarity, F, between these two embeddings phi of X, the embedding of the left hand signature, and phi of X prime, the embedding of the signature on the right. So the part of the model that needs training, is this neural net phi. And for that, the training data is used. The goal is to find a model phi that maximizes performance, on the training data that correctly classifies, similar pairs as being similar and dissimilar pairs, as being different. So, that's it. A very simple and intuitive architecture, for obtaining embeddings based only, on qualitative similarity information. And this was designed all the way back in 1993. Okay, so that's the end of our treatment of embeddings, based on distance and similarity information. This is a very active area of research, and a vital area for many applications of machine learning. 